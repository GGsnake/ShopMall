<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.superman.superman.dao.UserDao">
    <select id="selectByName" resultType="com.superman.superman.model.User">
		select * from hot_user where  loginName= #{username}
	</select>
	<!--<insert id="createUser" parameterType="User"-->
			<!--useGeneratedKeys="true" keyProperty="id">-->
        <!--insert into hot_user(userPhone,loginPwd)-->
             <!--values(#{user.userPhone},#{user.loginPwd})-->
    <!--</insert>-->
	<select id="selectByPhone" resultType="com.superman.superman.model.User">
		select * from hot_user where  userPhone= #{userPhone}
	</select>
	<insert id="createUser" >
		insert into hot_user(userPhone,loginPwd,createTime) value (#{user.userPhone},#{user.loginPwd},now())
	</insert>

	<update id="updateUser">
		update hot_user
		<set>
			<if test="user.userName != null">userName=#{user.userName},</if>
			<if test="user.loginPwd != null">loginPwd=#{user.loginPwd},</if>
			<if test="user.userScore != null">userScore=#{user.userScore},</if>
		</set>
		where id=#{user.id}
	</update>


	<update id="updateUserScore">
		update userinfo set userScore=userScore+ #{userScore}	where id=#{id}
	</update>
</mapper>